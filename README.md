# PyFinder
Un buscador de subdominios al cual le puedes cambiar la lista de palabras por si es muy corta xd 

el archivo que dice subdominios.txt es el encargado de hacer el request a las paginas si no estas seguro de las palabras las puedes cambiar o cambiar el archivo solo asegurate q este en la misma carpeta q el .py.
